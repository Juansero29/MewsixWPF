<Window x:Class="Mewsix.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:Mewsix.Helpers;assembly=Mewsix.Helpers"
        xmlns:vm="clr-namespace:Mewsix.ViewModels;assembly=Mewsix.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="379" Width="497.9">

    <Window.Resources>
        <conv:TabSizeConverter x:Key="tabSizeConverter" />
        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Width">
                <Setter.Value>
                    <MultiBinding Converter="{StaticResource tabSizeConverter}">
                        <Binding RelativeSource="{RelativeSource Mode=FindAncestor,
            AncestorType={x:Type TabControl}}" />
                        <Binding RelativeSource="{RelativeSource Mode=FindAncestor,
            AncestorType={x:Type TabControl}}" Path="ActualWidth" />
                    </MultiBinding>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>


    <Grid d:DataContext="{d:DesignInstance vm:MainWindowViewModel, IsDesignTimeCreatable=True}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*" MinWidth="210"/>
            <ColumnDefinition Width="6*"/>
        </Grid.ColumnDefinitions>
        <ListView Grid.Column="0" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" ItemsSource="{Binding Tracks}" MinWidth="200">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="0,0,0,1.5" BorderBrush="#FFE8E8E8" Background="White">
                        <Grid Margin="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="8*"/>
                            </Grid.ColumnDefinitions>

                            <Viewbox Grid.Column="0"  MaxWidth="80">
                                <Image Source="{Binding AlbumUri}" Stretch="UniformToFill"/>
                            </Viewbox>

                            <Grid Margin="4" Grid.Column="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="5*"/>
                                    <RowDefinition Height="5*"/>
                                </Grid.RowDefinitions>
                                <TextBlock VerticalAlignment="Bottom" Text="{Binding Name}" Grid.Row="0" Margin="0,0,0,4" FontFamily="Calibri Light" FontSize="14"/>
                                <TextBlock VerticalAlignment="Top" Text="{Binding Artist}" Grid.Row="1" Margin="0,0,0,4" FontFamily="Cambria Math" FontWeight="Bold"/>
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!--   RIGHT PART OF THE SCREEN  -->

        <TabControl Grid.Column="1">
            <TabItem Header="Lyrics">
                <TextBlock Grid.Column="1" Text="{Binding Lyrics}"/>
            </TabItem>
            <TabItem Header="Info">
                <Grid Margin="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*" />
                        <RowDefinition Height="1*"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="Info_ArtistName" Text="Name of the Artist"/>
                    <TextBlock Grid.Row="1" x:Name="Info_Track" Text="Track name"/>
                    
                    
                </Grid>

            </TabItem>
        </TabControl>

    </Grid>

</Window>
